const maintainer = String.fromEnvironment('maintainer', defaultValue: '未署名');

const email =
    String.fromEnvironment('email', defaultValue: 'undefined@example.foo');

const github = String.fromEnvironment('github',
    defaultValue:
        'https://github.com/lyexamination-undefined-maintainer-github-profile/undefined');

const maintainerGitHubProfile = String.fromEnvironment(
    'maintainerGitHubProfile',
    defaultValue:
        'https://github.com/lyexamination-undefined-maintainer-github-profile');
